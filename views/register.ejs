<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="/style.css">
  <title>Register</title>
</head>
<body>
  <div class="container">
    <!-- Left Panel -->
    <div class="left-panel">
      <h2>Register</h2>

      <% if (messages.error) { %>
        <div class="error"><%= messages.error %></div>
      <% } %>
      <% if (messages.success) { %>
        <div class="success"><%= messages.success %></div>
      <% } %>

      <form action="/register" method="POST">
        <div class="form-group">
          <input type="text" name="firstName" required placeholder="First name" class="input-field">
        </div>
        <div class="form-group">
          <input type="text" name="lastName" required placeholder="Last name" class="input-field">
        </div>
        <div class="form-group">
          <input type="text" name="email" required placeholder="Email" class="input-field">
        </div>
        <div class="form-group">
          <input type="text" name="username" required placeholder="Username" class="input-field">
        </div>
        <div class="form-group">
          <input type="password" name="password" required placeholder="Password" class="input-field">
        </div>
        <div class="form-group">
          <input type="text" name="keyword" required placeholder="Keyword for doctors and operators" class="input-field">
        </div>
        <button type="submit" class="submit-btn">Register</button>
      </form>
      <p>Already have an account? <a href="/login">Login here</a></p>
    </div>

    <!-- Right Panel - Maybe add some description or instructions) -->
    <div class="right-panel">
      <div class="chat-container">
        <div class="chat-header">
          <h1>Welcome to the Healthcare System</h1>
          <p>Create an account to continue</p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
